# Check: business-brain has classifiable content

Run during Phase 2 BEFORE smart-suggestion.

The classifier needs at least:
- `## Role` OR `## What I do` section with content
- `## Audience` OR `## ICP` section with content
- `## Offer` OR `## What I sell` section with content

If ANY of the three is empty:
- Say: "Your business-brain is missing <which one>. Fill it in via `/onboard` Phase 3, then come back."
- HALT.
