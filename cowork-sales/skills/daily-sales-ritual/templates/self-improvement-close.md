# Self-improvement close — every skill, every run

After delivering the main output + Next Move block, ask:

> "What would've made this 10% better?"

Accept a one-line answer. Then:

1. Append to `projects/sales/skill-memory.md`:
   ```
   <YYYY-MM-DD> | /<skill-name> | <answer verbatim>
   ```

2. Read `skill-memory.md` and check if any pattern recurs 3+ times for this skill name. Patterns matching by:
   - Substring overlap >= 60% with prior entries
   - Same keyword (e.g., "buyer pyramid", "Gmail", "Fathom", "tactic")

3. If recurrence detected:
   - Surface: "I've seen this 3+ times. Want me to update `/<skill-name>` itself?"
   - If yes → draft change to `projects/sales/skill-improvements.md` in this format:
     ```
     | /<skill-name> | <pattern> | <first_seen_date> | <recurrence_count> | <suggested_change> | <reviewed: no> |
     ```

4. If no recurrence → silent. No noise.
